# Cargo

## プロジェクトの作成

```shell
$ cargo new [Project_Name] --bin
```

## ビルド

```shell
$ cargo build
$ cargo build --release # リリース用実行ファイルの作成
$ cargo check           # 実行用ファイルを生成しない
```

## 実行

```shell
$ cargo run
```
